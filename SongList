public class SongList {
    private static final int MAX = 50;  // sets the maximum number of elements (songs) in the SongList array
    private Song[] songList;            // a variable used when creating a new array of songs
    private int size;                   // a counter to keep track of the number of songs added to the SongList 

/**
 * This is the constructor for SongList that creates an array to which songs will be added and sorted.
 */
public SongList(){
    songList = new Song[MAX];
    size = 0;
}

public void addSong(Song plus1){  //asking do you have another space to add another song?
    songList[size] = plus1;       // add the song to SongList
    size++;                       // add one to the counter keeping track of the number of songs in the list
    }

public int findSong(String title){
    for (int i = 0; )
    return i;
}    
}
/**
* I have this commented out for now until I figure out how to write them


    public void swapSong(int, int){
    
}
    
    public void sortByWriter(){
        
    }
    
    public void sortByDuration(){
        
    }
    
    public void sortByGenre(){
        
    }
    
    public void sortByYear(){
        
    }
    
    public int getSize(){
        return size;
    }
*/
